/** Absolute origin this app is served from. Canonicals, JSON-LD and og:url
 *  must be absolute, and they must point at production regardless of whether
 *  the code is running on localhost. */
export const APP_URL = "https://app.letsellr.in";

export const SITE_NAME = "Letsellr";

/** Marketing site. Canonical host is www — the apex 308-redirects to it. */
export const SITE_URL = "https://www.letsellr.in";
